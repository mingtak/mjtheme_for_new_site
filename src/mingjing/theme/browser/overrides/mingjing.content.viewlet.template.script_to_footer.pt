<div id="script_to_footer">

<!-- Live_slider -->
<script>
    $(document).ready(function(){
        var slider = new MasterSlider();
        slider.setup('cover_live_slider_1' , {
            width:1130,
            height:500,
            autoplay:true,
            loop:true,
            space:10,
            speed: 10,
            preload:1,
            view:'basic'
        });
        slider.control('arrows');

        var gallery = new MSGallery('ms-gallery-cover_live_slider_1' , slider);
        gallery.setup();
    });
</script>


<!-- Twitter embed code -->
<script async src="//platform.twitter.com/widgets.js" charset="utf-8"></script>


<!-- 繁簡轉換 -->
<script tal:condition="python:'folder_contents' not in view.request.getURL()">
$(function() {
    var lang = getCookie('lang');
    if(lang == 'tw'){     
        $("body").s2t();
        document.getElementById('portaltab-s2t').innerHTML = '简';
    }else{
        $("body").t2s();
        document.getElementById('portaltab-s2t').innerHTML = '繁';
    }
});

$("#portaltab-s2t").click(function(){
    var lang = getCookie('lang');
    if(lang == 'tw'){
        setCookie('lang', 'cn');
        $("body").t2s();
        document.getElementById('portaltab-s2t').innerHTML = '繁';
    }else{
        setCookie('lang', 'tw');
        $("body").s2t();
        document.getElementById('portaltab-s2t').innerHTML = '简';
    }
});

function setCookie(cname, cvalue, exdays) {
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+ d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
}

function getCookie(cname) {
    var name = cname + "=";
    var ca = document.cookie.split(';');
    for(var i = 0; i <ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "tw";
}
</script>

</div>
